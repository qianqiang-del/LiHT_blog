# 2026-08-19 工作日志

## 前台模块接口设计（仅设计，不写代码）
- 用户明确：**不用写代码，只用设计**；前端优先用 mock 数据对接。
- 所有设计文档统一约定：复用基建 `pkg/response`（`response.Success/BizError`）、`pkg/errors`（`errors.New/NewDefault`、`CodeXxx`）、`response.PageRequest/PageResponse`（分页字段 `page`+`size` 必填，响应 `list/total/page/size/total_page`），**不自定义分页结构**。

### 已产出文档（docs/）
1. `首页接口设计.md`（v1.2）—— 3 个 GET：`/api/v1/articles`（数据流，分页+sort+category_id/tag_id/keyword）、`/api/v1/author`（作者信息+stats）、`/api/v1/hot`（热门排行，limit）。
2. `分类模块接口设计.md`（v1.0）—— 2 个 GET：`/api/v1/categories`（列表含文章数）、`/api/v1/categories/:id/articles`（分类下文章流）。分类实体仅 `name`，无 sort，按 id 升序。
3. `标签模块接口设计.md`（v1.0）—— 2 个 GET：`/api/v1/tags`（列表含文章数，按热度降序）、`/api/v1/tags/:id/articles`。与分类同构，但经 `article_tags` 多对多；前置依赖：`article_repository.ListPublished` 需支持 `tag_id` 筛选。
4. `关于模块接口设计.md`（v1.0）—— 1 个聚合 GET：`/api/v1/about`（作者简介 + skill_groups 树 + contacts + stories）。数据源：`users`(作者) + `site_items`(type 区分，含 sort 排序)。

### 关键实体事实
- `site_items` 字段：type/title/content/parent_id(skill->分组)/code/icon/sort。
- `ArticleListItem` 复用：id/title/summary/cover/category/tags/author/view_count/like_count/comment_count/hot/published_at。

## 待办（设计未完成模块）
- 前台：文章详情+评论+点赞、认证（登录/注册/me）、搜索。
- 后台管理端：概览/文章/分类/标签/评论/用户/设置（admin-prototype.html）。

## 种子数据（docs/seed.sql 重新生成）
- 数据库名 `liht`（config.yaml）；用户实体已手动加 `github` 列。
- 原 seed.sql 问题：INSERT 缺 created_at/updated_at（GORM 建列 NOT NULL 无默认值）；缺 comments/article_likes/comment_likes/site_items 数据。
- 新版覆盖 9 张表：users(3)/categories(5)/tags(12)/articles(12)/article_tags/article_likes(9)/comments(11 含嵌套回复)/comment_likes(12)/site_items(19，skill_group+skill+contact+story)。
- 所有用户密码 bcrypt("password")；导入命令 `mysql -u root -p liht < docs/seed.sql`，文件内置 TRUNCATE 清空。
- **已成功导入 MySQL**（root/123456，mysql 8.0.44，库 liht）：users 3 / categories 5 / tags 12 / articles 12 / article_tags 19 / comments 11 / article_likes 9 / comment_likes 12 / site_items 19。抽查 github/hot/site_items.type 均正常。
